# Copy this file to .env and fill in real values. .env is gitignored.

# Kraken public REST is unauthenticated for the endpoints we use; no key needed.
# Binance public REST is unauthenticated for /api/v3/aggTrades and /api/v3/klines; no key needed.
# Add other source keys here as adapters get built.
